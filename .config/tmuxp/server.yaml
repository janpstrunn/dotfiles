session_name: "server"
windows:
  - layout: tiled
    options: {}
    panes:
      - focus: "true"
        shell_command_before:
          - pkill emacs
          - doom sync
        # shell_command: emacs --daemon
      - shell_command: alacritty --daemon
    start_directory: $HOME
    window_name: server
